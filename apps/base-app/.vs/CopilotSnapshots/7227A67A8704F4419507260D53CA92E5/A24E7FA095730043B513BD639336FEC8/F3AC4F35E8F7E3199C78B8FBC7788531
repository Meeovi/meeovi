import { ref } from 'vue'
import { useNuxtApp } from 'nuxt/app'

export const useCommerceProduct = (id) => {
  const { $commerce } = useNuxtApp()
  const product = ref(null)

  const load = async () => {
    product.value = await $commerce.getProduct(id)
  }

  return { product, load }
}
